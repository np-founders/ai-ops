# AI Ops — AI workflows running on nullplatform

Public landing showcasing the five AI workflows the nullplatform team runs on
its own production: Deployments Analyzer, Library Analyzer, Cost Intelligence,
Self-Documenter and Smart Checklists.

**Live:** https://np-founders.github.io/ai-ops/

Static site (no build): `index.html` + `styles.css` + `assets/`. Design spec in
`docs/superpowers/specs/`. Edit and push to `main` to deploy.
